springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

wedu:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}

logging:
  level:
    com.wedu: INFO
    org.hibernate.SQL: WARN
